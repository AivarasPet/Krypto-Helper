<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="meh.scripts">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/improvised"
        android:label="Krypto helper"
        android:supportsRtl="true"
        android:theme="@style/LightAppTheme">
        <activity android:name="meh.scripts.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="meh.scripts.DisplayMessageActivity"
            android:parentActivityName="meh.scripts.MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <!-- GRAPH -->
        <activity android:name="meh.scripts.GraphActivity" />

        <!-- MONEY -->

        <!-- NEWS -->

        <!-- OPTIONS -->
        <activity
            android:name="meh.scripts.OptionsActivity"
            android:screenOrientation="fullSensor" />

        <!--
             INFO
            <activity android:name=".InfoActivity" />
        -->

        <activity android:name="meh.scripts.WebsiteActivity" />
        <activity android:name="meh.scripts.InfoActivity" />
        <activity
            android:name="meh.scripts.bottombar"
            android:label="bottom"></activity>
    </application>

</manifest>